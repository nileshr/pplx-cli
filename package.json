{
  "name": "pplx-cli",
  "version": "0.2.1",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.8"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1"
  },
  "scripts": {
    "build": "bun build ./index.ts --compile --outfile pplx",
    "release": "bun run build && sudo cp pplx /usr/local/bin/"
  }
}
