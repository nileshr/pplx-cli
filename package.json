{
  "name": "pplx-cli",
  "version": "1.0.0",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^12.0.2",
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.8",
    "semantic-release": "^25.0.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1"
  },
  "scripts": {
    "build": "bun build ./index.ts --compile --outfile pplx",
    "install:local": "bun run build && sudo cp pplx /usr/local/bin/"
  }
}
